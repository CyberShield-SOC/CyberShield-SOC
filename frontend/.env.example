# Use /api to connect through the Vite development proxy to FastAPI on port 3000.
# Leave empty to use the deterministic local sample repository instead.
# Never put credentials or secrets in VITE_* variables.
VITE_API_BASE_URL=/api
