/**
 * Group A/B/C built-in detection rules. Mirrors backend/app/detection/rules —
 * engineKey is the backend rule name, `defaults` the class defaults (used by
 * sample mode and as the editor's fallback), `logSource` what the rule needs
 * to have any data to evaluate.
 */

function rule(definition) {
  return Object.freeze({
    category: "generic_detection",
    executable: true,
    version: "Built-in",
    status: "enabled",
    lastUpdated: "Current deployment",
    ...definition,
  });
}

export const EXTENDED_DETECTION_RULES = Object.freeze({
  // ── Group A: post-compromise and log integrity ──────────────────────────────
  "R-A01": rule({
    id: "R-A01",
    engineKey: "new_account_created",
    group: "A",
    name: "New account created",
    description: "A new operating-system account was created with useradd or adduser.",
    technique: "T1136 · Create Account",
    severity: "high",
    owner: "Linux Security",
    criteria: "Any useradd/adduser event; accounts created on one host within 30 minutes are reported together",
    input: "Syslog events from the useradd/adduser processes",
    groupBy: "Host",
    query: "process in (useradd, adduser) message ~ name=<account> | merge by hostname within cooldown",
    response: "Confirm the account was requested through change management; disable it and review who created it if not.",
    logSource: "Syslog (auth.log)",
    defaults: { cooldown_seconds: 1800 },
  }),
  "R-A02": rule({
    id: "R-A02",
    engineKey: "privileged_group_modified",
    group: "A",
    name: "Privileged group modified",
    description: "An account was added to a privileged group such as sudo, wheel, admin, root, or docker.",
    technique: "T1098 · Account Manipulation",
    severity: "high",
    owner: "Linux Security",
    criteria: "usermod/gpasswd adds an account to a group on the privileged-group allowlist",
    input: "Syslog events from usermod/gpasswd",
    groupBy: "Host",
    query: "process in (usermod, gpasswd) message ~ \"add <user> to group <group>\" | group in allowlist",
    response: "Verify the membership change was approved, remove it if not, and review activity by the account since.",
    logSource: "Syslog (auth.log)",
    defaults: { cooldown_seconds: 1800, allowlist: ["sudo", "wheel", "admin", "root", "docker"] },
  }),
  "R-A03": rule({
    id: "R-A03",
    engineKey: "cron_persistence",
    group: "A",
    name: "Cron or timer persistence",
    description: "A crontab was replaced or edited, or a systemd timer was started.",
    technique: "T1053.003 · Cron",
    severity: "medium",
    owner: "Linux Security",
    criteria: "crontab REPLACE/EDIT or systemd 'Started <name>.timer'; routine cron job runs and crontab -l are ignored",
    input: "Syslog events from crontab and systemd",
    groupBy: "Host",
    query: "(process=crontab message ~ \"(user) REPLACE|EDIT\") OR (process=systemd message ~ \"Started *.timer\")",
    response: "Inspect the new cron entry or timer unit, confirm its owner, and remove unapproved scheduled tasks.",
    logSource: "Syslog",
    defaults: { cooldown_seconds: 1800 },
  }),
  "R-A04": rule({
    id: "R-A04",
    engineKey: "security_control_disabled",
    group: "A",
    name: "Security control disabled",
    description: "A firewall was flushed or disabled, SELinux/AppArmor enforcement was relaxed, or a security agent was stopped.",
    technique: "T1562 · Impair Defenses",
    severity: "high",
    owner: "Linux Security",
    criteria: "systemd stops a watched security service; iptables -F, ufw disable, nft flush, setenforce 0, aa-teardown commands; audit MAC_STATUS enforcing=0",
    input: "Syslog (systemd, sudo COMMAND=) and Linux audit records",
    groupBy: "Host",
    query: "systemd Stopped <watched service> OR command ~ firewall/MAC disable OR audit MAC_STATUS enforcing=0",
    response: "Re-enable the control immediately, identify who disabled it, and look for activity it may have been hiding.",
    logSource: "Syslog with sudo logging, and/or auditd",
    defaults: { cooldown_seconds: 900, allowlist: ["auditd", "apparmor", "fail2ban", "ufw", "firewalld", "clamav-daemon"] },
  }),
  "R-A05": rule({
    id: "R-A05",
    engineKey: "log_tampering",
    group: "A",
    name: "Log tampering",
    description: "Audit or logging was switched off, the journal was cleared, or shell history / system logs were deleted or truncated.",
    technique: "T1070 · Indicator Removal",
    severity: "critical",
    owner: "Linux Security",
    criteria: "logging service stopped, journal vacuumed, auditctl -D / -e 0, history/log files deleted or truncated, HISTFILE disabled",
    input: "Syslog (systemd, journald, sudo COMMAND=) and Linux audit records",
    groupBy: "Host",
    query: "Stopped <log service> OR journald vacuum OR command ~ history/log wipe OR audit CONFIG_CHANGE remove_rule / DAEMON_END",
    response: "Treat the host as compromised: preserve remaining evidence, pull logs from central collection, and investigate the actor.",
    logSource: "Syslog with sudo logging, and/or auditd",
    setupNote: "An interactive `history -c` is a shell builtin and is never logged; it is only visible when run through `bash -c` under auditd.",
    defaults: { cooldown_seconds: 900, allowlist: ["auditd", "rsyslog", "rsyslogd", "syslog-ng", "systemd-journald"], params: { ignored_processes: "logrotate,systemd-tmpfiles,journald" } },
  }),
  "R-A06": rule({
    id: "R-A06",
    engineKey: "ssh_key_added",
    group: "A",
    name: "SSH authorized key added",
    description: "An SSH authorized_keys file was written, created, replaced, or deleted.",
    technique: "T1098.004 · SSH Authorized Keys",
    severity: "high",
    owner: "Linux Security",
    criteria: "Audit write/create/rename on */.ssh/authorized_keys (reads by sshd ignored), or a command writing to it",
    input: "Linux audit records (a watch such as -w /root/.ssh/authorized_keys -p wa -k ssh_keys), sudo COMMAND=",
    groupBy: "Host",
    query: "audit PATH name ~ /.ssh/authorized_keys AND (write flags OR CREATE/DELETE OR watch key) OR command ~ tee/cp/>> authorized_keys",
    response: "Review the added key, remove it if unknown, rotate access for the account, and check for other persistence.",
    logSource: "auditd (sshd never logs key changes)",
    setupNote: "Needs an audit watch on authorized_keys files; without auditd only sudo-run commands are visible.",
    defaults: { cooldown_seconds: 900, params: { watch_keys: "ssh_keys,ssh_key,sshd_keys,authorized_keys" } },
  }),
  "R-A07": rule({
    id: "R-A07",
    engineKey: "host_log_silence",
    group: "A",
    name: "Host log silence",
    description: "A host with an established reporting cadence stopped sending logs.",
    technique: "T1562.006 · Indicator Blocking",
    severity: "high",
    owner: "Detection Engineering",
    criteria: "Gap longer than 5× the host's usual interval (minimum 30 minutes, maximum 24 hours) after 20+ events",
    input: "Any events carrying a hostname; state kept in host heartbeats",
    groupBy: "Host",
    query: "per hostname: gap > max(window, threshold × p90 interval) | ongoing silence checked on upload or via 'Check for silent hosts'",
    response: "Check whether the host is down, its log agent stopped, or logging was deliberately disabled.",
    logSource: "Any source with a host name",
    setupNote: "Nothing runs on a schedule: ongoing silence is noticed on the next upload or when 'Check for silent hosts' is run (an external cron can call POST /detection/host-silence/check).",
    defaults: { threshold: 5, window_seconds: 1800, cooldown_seconds: 0, params: { min_events: 20, max_silence_seconds: 86400 } },
  }),

  // ── Group B: identity context ───────────────────────────────────────────────
  "R-B01": rule({
    id: "R-B01",
    engineKey: "direct_root_login",
    group: "B",
    name: "Direct root login",
    description: "A successful login straight into a superuser account.",
    technique: "T1078.003 · Local Accounts",
    severity: "high",
    owner: "Identity Security",
    criteria: "Successful login whose username exactly matches a superuser name (default: root)",
    input: "SUCCESS login_attempt events",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt username in allowlist",
    response: "Confirm who logged in, disable direct root login (PermitRootLogin no), and require named accounts with sudo.",
    logSource: "Any authentication log",
    defaults: { cooldown_seconds: 0, allowlist: ["root"] },
  }),
  "R-B02": rule({
    id: "R-B02",
    engineKey: "service_account_interactive",
    group: "B",
    name: "Service account used interactively",
    description: "A service or system account completed an interactive login.",
    technique: "T1078 · Valid Accounts",
    severity: "high",
    owner: "Identity Security",
    criteria: "Successful login by an account on the service-account allowlist",
    input: "SUCCESS login_attempt events",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt username in allowlist",
    response: "Service accounts should not log in interactively: rotate its credentials and find where they were used from.",
    logSource: "Any authentication log",
    setupNote: "Does nothing until you list your service accounts in the allowlist.",
    requiresAllowlist: true,
    defaults: { cooldown_seconds: 0, allowlist: [] },
  }),
  "R-B03": rule({
    id: "R-B03",
    engineKey: "login_to_nonexistent_account",
    group: "B",
    name: "Login to decommissioned account",
    description: "A login attempt targeted an account known to be deleted or disabled.",
    technique: "T1078 · Valid Accounts",
    severity: "medium",
    owner: "Identity Security",
    criteria: "Failed login for a username on the decommissioned-account allowlist",
    input: "FAILED login_attempt events",
    groupBy: "Account",
    query: "status=FAILED event_type=login_attempt username in allowlist",
    response: "Someone still holds old credentials: find the source, and confirm the account really is removed everywhere.",
    logSource: "Any authentication log",
    setupNote: "Logs can't tell a deleted account from one that never existed, so this needs a roster of decommissioned accounts.",
    requiresAllowlist: true,
    defaults: { cooldown_seconds: 0, allowlist: [] },
  }),
  "R-B04": rule({
    id: "R-B04",
    engineKey: "off_hours_login",
    group: "B",
    name: "Off-hours login",
    description: "A successful login outside the configured business-hours window.",
    technique: "T1078 · Valid Accounts",
    severity: "low",
    owner: "Identity Security",
    criteria: "Successful login outside 06:00–20:00 UTC (fixed window, not a learned baseline)",
    input: "SUCCESS login_attempt events with a timestamp",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt | hour(timestamp) not in [start_hour, end_hour)",
    response: "Check the user's schedule and on-call status before escalating.",
    logSource: "Any authentication log",
    defaults: { cooldown_seconds: 0, start_hour: 6, end_hour: 20 },
  }),
  "R-B05": rule({
    id: "R-B05",
    engineKey: "dormant_account_activity",
    group: "B",
    name: "Dormant account activity",
    description: "An account that has been idle for a long time suddenly authenticated.",
    technique: "T1078 · Valid Accounts",
    severity: "medium",
    owner: "Identity Security",
    criteria: "Successful login 30+ days after the account's previous login (tracked across uploads)",
    input: "SUCCESS login_attempt events; last-login history in entity baselines",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt | now - last_login(username) >= threshold days",
    response: "Confirm the owner is back; otherwise disable the account and review what it accessed.",
    logSource: "Any authentication log",
    setupNote: "Never fires the first time an account is seen.",
    defaults: { cooldown_seconds: 0, threshold: 30 },
  }),
  "R-B06": rule({
    id: "R-B06",
    engineKey: "impossible_travel",
    group: "B",
    name: "Impossible travel",
    description: "An account logged in from two places too far apart for the time between the logins.",
    technique: "T1078 · Valid Accounts",
    severity: "high",
    owner: "Identity Security",
    criteria: "Consecutive successful logins ≥ 500 km apart implying > 1,000 km/h",
    input: "SUCCESS login_attempt events with latitude/longitude for public source IPs",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt | distance(prev, current) / elapsed > max_speed_kmh",
    response: "Assume the credentials are shared or stolen: revoke sessions, reset the password, and confirm MFA.",
    logSource: "Auth logs with geo columns, or a local GeoIP database (GEOIP_CITY_DB_PATH)",
    setupNote: "Needs coordinates: from the log itself or a configured MaxMind-format database. No network lookups are made.",
    defaults: { cooldown_seconds: 3600, allowlist: [], params: { max_speed_kmh: 1000, min_distance_km: 500 } },
  }),
  "R-B07": rule({
    id: "R-B07",
    engineKey: "first_seen_geo_asn",
    group: "B",
    name: "Login from new country or network",
    description: "An account logged in from a country or ASN it has never used before.",
    technique: "T1078 · Valid Accounts",
    severity: "medium",
    owner: "Identity Security",
    criteria: "Successful login from an unseen country or ASN after at least 5 prior geolocated logins",
    input: "SUCCESS login_attempt events with country/ASN; per-account history in entity baselines",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt | country not in known(username) OR asn not in known(username)",
    response: "Confirm travel or a new ISP with the user before escalating.",
    logSource: "Auth logs with geo columns, or a local GeoIP database (GEOIP_CITY_DB_PATH / GEOIP_ASN_DB_PATH)",
    defaults: { cooldown_seconds: 3600, allowlist: [], params: { min_history: 5 } },
  }),

  // ── Group C: network and lateral movement ───────────────────────────────────
  "R-C01": rule({
    id: "R-C01",
    engineKey: "lateral_movement_chain",
    group: "C",
    name: "Lateral movement chain",
    description: "The same account authenticated successfully on several hosts in quick succession.",
    technique: "T1021.004 · SSH",
    severity: "high",
    owner: "Detection Engineering",
    criteria: "One account logs in to 3+ distinct hosts within 600 seconds (tracked across uploads)",
    input: "SUCCESS login_attempt events with the host logged in to",
    groupBy: "Account",
    query: "status=SUCCESS event_type=login_attempt | window 600s by username | distinct(hostname) >= 3",
    response: "Map the hosts touched, check how the account reached each, and contain it if the path isn't expected.",
    logSource: "Syslog auth logs from multiple hosts",
    defaults: { threshold: 3, window_seconds: 600, cooldown_seconds: 0 },
  }),
  "R-C02": rule({
    id: "R-C02",
    engineKey: "host_sweep",
    group: "C",
    name: "Host sweep",
    description: "One source IP probed the same port across many hosts — the horizontal counterpart of a port scan.",
    technique: "T1046 · Network Service Discovery",
    severity: "medium",
    owner: "Network Security",
    criteria: "One source contacts the same destination port on 10+ distinct hosts within 300 seconds",
    input: "Connection events with source IP, destination IP, and destination port",
    groupBy: "Source IP",
    query: "window 300s by (source_ip, dest_port) | distinct(dest_ip) >= 10",
    response: "Block the source, and check the probed service on every host it reached for follow-on exploitation.",
    logSource: "Firewall logs (UFW/iptables) or flow exports",
    defaults: { threshold: 10, window_seconds: 300, cooldown_seconds: 1800, allowlist: [] },
  }),
  "R-C03": rule({
    id: "R-C03",
    engineKey: "outbound_beaconing",
    group: "C",
    name: "Outbound beaconing",
    description: "An internal host connected to one external destination at regular, low-jitter intervals.",
    technique: "T1071 · Application Layer Protocol",
    severity: "high",
    owner: "Network Security",
    criteria: "10+ connections to one external IP:port, median interval 10s–2h, jitter ≤ 15%",
    input: "Connection events from internal source IPs to public destination IPs",
    groupBy: "Host",
    query: "by (source_ip, dest_ip, port) | count >= 10 | stdev(interval)/mean(interval) <= max_jitter_percent",
    response: "Isolate the host, identify the calling process, and block the destination.",
    logSource: "Firewall logs or flow/proxy exports",
    defaults: { threshold: 10, cooldown_seconds: 21600, allowlist: [], params: { max_jitter_percent: 15, min_interval_seconds: 10, max_interval_seconds: 7200, dedupe_seconds: 2 } },
  }),
  "R-C04": rule({
    id: "R-C04",
    engineKey: "dns_tunneling",
    group: "C",
    name: "DNS tunneling",
    description: "A client sent many long, high-entropy DNS queries to one parent domain.",
    technique: "T1071.004 · DNS",
    severity: "high",
    owner: "Network Security",
    criteria: "20+ distinct queries with 30+ character, ≥ 3.5 bits/char subdomains under one parent domain within 600 seconds",
    input: "DNS query events with client IP and query name",
    groupBy: "Source IP",
    query: "len(subdomain) >= min_subdomain_length AND entropy(subdomain) >= min_entropy | window 600s by (client, parent) | distinct >= 20",
    response: "Block the parent domain, isolate the client, and review what data the queries could have carried.",
    logSource: "DNS server logs (dnsmasq, BIND, Unbound) or DNS exports",
    defaults: { threshold: 20, window_seconds: 600, cooldown_seconds: 3600, allowlist: [], params: { min_subdomain_length: 30, min_entropy: 3.5 } },
  }),
  "R-C05": rule({
    id: "R-C05",
    engineKey: "egress_volume_anomaly",
    group: "C",
    name: "Egress volume anomaly",
    description: "A host sent far more outbound data than its own baseline.",
    technique: "T1041 · Exfiltration Over C2 Channel",
    severity: "high",
    owner: "Network Security",
    criteria: "Hourly outbound bytes ≥ 5× and > mean + 3σ of the host's baseline, at least 50 MB, after 6 baseline hours",
    input: "Flow events with bytes_out to public destinations; per-host baseline in entity baselines",
    groupBy: "Host",
    query: "sum(bytes_out) per host per window | > multiplier × mean AND > mean + stddevs × σ AND >= min_bytes",
    response: "Identify the destination and the process sending the data; contain the host if the transfer isn't approved.",
    logSource: "Flow or proxy exports with byte counts",
    defaults: { window_seconds: 3600, cooldown_seconds: 0, allowlist: [], params: { multiplier: 5, stddevs: 3, min_bytes: 50000000, min_samples: 6 } },
  }),
  "R-C06": rule({
    id: "R-C06",
    engineKey: "threat_intel_match",
    group: "C",
    name: "Threat intelligence match",
    description: "Traffic or DNS activity matched an indicator from an imported threat-intelligence feed.",
    technique: "T1071 · Application Layer Protocol",
    severity: "high",
    owner: "Threat Intelligence",
    criteria: "Source IP, destination IP, domain, or DNS query matches an imported IP, CIDR, or domain (including subdomains)",
    input: "Network, proxy, and DNS events; indicators from imported feeds",
    groupBy: "Host",
    query: "ip_address OR dest_ip in feed IPs/CIDRs OR domain/dns_query under feed domains",
    response: "Validate the indicator, block it, and scope every host that contacted it.",
    logSource: "Firewall, flow, proxy, or DNS logs",
    setupNote: "Does nothing until at least one feed is imported below.",
    defaults: { cooldown_seconds: 3600, allowlist: [] },
  }),

  // ── Group D: machine-learning pilot ─────────────────────────────────────────
  "R-D01": rule({
    id: "R-D01",
    engineKey: "behavioral_anomaly_login",
    group: "D",
    name: "Unusual login behavior (ML)",
    description: "A login's time-of-day, day-of-week, and new-country combination scored as unusual against a trained IsolationForest model — a learned, joint judgment neither off_hours_login nor first_seen_geo_asn expresses alone.",
    technique: "T1078 · Valid Accounts",
    severity: "low",
    owner: "Detection Engineering",
    criteria: "decision_function score below a configured threshold against the account population's trained model",
    input: "SUCCESS login_attempt events; scored against a model trained offline from stored feature snapshots",
    groupBy: "Account",
    query: "IsolationForest(hour_sin, hour_cos, dow_sin, dow_cos, is_new_geo).decision_function(login) < score_threshold",
    response: "Advisory only — review alongside the account's other activity before acting; this is a statistical judgment, not a rule match.",
    logSource: "Any authentication log (same as off_hours_login)",
    setupNote: "Every login is logged as training data from day one. Two gates before this ever alerts: (1) no trained model yet — pure data collection; (2) once trained via POST /ml/models/login_behavior/train, `params.shadow_mode` (default on) still scores and stores every event but withholds alerts until an admin reviews GET /ml/scores and turns it off. Needs real volume: a lab/demo deployment won't have enough history for a meaningful model for a while.",
    defaults: { cooldown_seconds: 3600, params: { score_threshold: -0.02, shadow_mode: true } },
  }),

  "R-D02": rule({
    id: "R-D02",
    engineKey: "behavioral_anomaly_egress",
    group: "D",
    name: "Unusual egress behavior (ML)",
    description: "A host's hourly egress bucket — bytes out, connection count, distinct destinations, and time of day — scored jointly against a trained IsolationForest model, catching combinations that stay under egress_volume_anomaly's single-dimension threshold on their own.",
    technique: "T1041 · Exfiltration Over C2 Channel",
    severity: "low",
    owner: "Detection Engineering",
    criteria: "decision_function score below a configured threshold against the host population's trained model",
    input: "Flow/proxy records with byte counts (dest_ip, bytes_out); bucketed hourly per host",
    groupBy: "Host",
    query: "IsolationForest(log_bytes_out, connection_count, distinct_destinations, hour_sin, hour_cos).decision_function(bucket) < score_threshold",
    response: "Advisory only — review alongside egress_volume_anomaly and outbound_beaconing before acting; this is a statistical judgment, not a rule match.",
    logSource: "Same CSV/JSON flow or proxy exports egress_volume_anomaly needs; firewall packet logs alone don't carry byte counts.",
    setupNote: "Same two-gate shadow mode as R-D01: no model yet means pure snapshot collection; once trained via POST /ml/models/egress_volume/train, `params.shadow_mode` (default on) withholds alerts until reviewed at GET /ml/scores and turned off.",
    defaults: { cooldown_seconds: 3600, params: { score_threshold: -0.02, shadow_mode: true } },
  }),
});
